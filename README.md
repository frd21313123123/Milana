# Telegram-клиент на Python

CLI-программа подключается к обычному пользовательскому аккаунту Telegram через
MTProto и библиотеку Telethon. Она умеет показывать диалоги, читать историю,
отправлять текстовые сообщения, выводить новые сообщения в реальном времени и
запускать ИИ-ответчик для одного разрешённого пользователя.

> Используйте программу только со своим аккаунтом. Не публикуйте `.env` и файл
> `sessions/telegram_account.session`: файл сессии даёт доступ к аккаунту.

## Установка

Требуется Python 3.10 или новее.

```powershell
cd D:\python-w1\AI-goi
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

Данные `api_id` и `api_hash` со скриншота уже записаны в локальный `.env`.
Файл исключён из Git.

## Первый вход

```powershell
python telegram_client.py login
```

Telethon запросит номер телефона в международном формате, код из Telegram и,
если включена двухэтапная аутентификация, облачный пароль. После входа рядом со
скриптом появится защищаемый файл `sessions/telegram_account.session`, и повторно
вводить код обычно не потребуется.

## Команды

```powershell
# Показать подключённый аккаунт
python telegram_client.py me

# Последние 30 диалогов и их ID
python telegram_client.py dialogs --limit 30

# Последние 20 сообщений (username или числовой ID из команды dialogs)
python telegram_client.py read @username --limit 20
python telegram_client.py read -1001234567890 --limit 20

# Отправить сообщение; текст с пробелами можно передать без кавычек
python telegram_client.py send @username Привет! Это тест.

# Слушать новые сообщения во всех диалогах или только в одном
python telegram_client.py listen
python telegram_client.py listen @username

# Запустить ИИ-ответчик для всех входящих сообщений
python telegram_client.py ai-bot

# Запустить прямое DEV-общение без расписания и искусственных пауз
python telegram_client.py ai-bot --dev-chat

# Показать текущее состояние и расписание Миланы
python telegram_client.py schedule
python telegram_client.py schedule --brief
python telegram_client.py schedule --day mon
```

## ИИ-ответчик

Команда `ai-bot` реагирует на все входящие текстовые сообщения и фото, а при
выбранном Gemini — также на GIF-анимации, голосовые сообщения и поддерживаемые
видео. Каждому
чату принадлежит отдельная очередь и один worker, поэтому разные чаты ожидают и
генерируют ответы независимо. Первое сообщение остаётся непрочитанным до
естественного момента ответа по расписанию. Затем клиент ждёт две секунды тишины
(но не больше восьми дополнительных секунд) и объединяет все накопившиеся
сообщения этого чата в один пакет. Отметка о прочтении ставится сразу до
последнего сообщения пакета, а первая часть ответа привязывается именно к нему.

Если новая реплика приходит, пока модель формирует ответ, устаревший черновик не
отправляется: генерация запускается заново со всем накопленным пакетом. Милана
сама выбирает, ответить одним сообщением или естественной серией до пяти
сообщений. Между частями выдерживается короткая пауза с индикатором
«печатает…». Если пользователь пишет во время этой паузы, неотправленный остаток
серии отменяется, а новое сообщение становится началом продолжения диалога.

Милана также может поставить одну реакцию на последнее сообщение собранного
пакета: `👍`, `❤`, `🔥`, `🤣`, `😢`, `🎉` или `🤔`. Реакция может заменить текст
для простого подтверждения либо сопровождать содержательный ответ и в таком
случае отправляется первой. Если реакции отключены в конкретном чате, ошибка
логируется, но подготовленный текст всё равно отправляется. Реакции не
сохраняются как отдельные текстовые сообщения в локальной истории.

Милана может по собственному решению добавить автора последнего входящего
сообщения в чёрный список Telegram. Это действие предназначено для серьёзных
угроз, травли, систематических оскорблений и навязчивого спама, а не для обычного
несогласия или единичной грубости. Если выбрана прощальная реплика, она
отправляется до блокировки. Решение применяется только к автору сообщения (в
том числе в группе), а не ко всему чату, и не выполняется из устаревшего
черновика, отменённого новой входящей репликой.

Исходящие сообщения самого аккаунта игнорируются, чтобы не возникал цикл
ответов. Telegram-фото, а также JPEG, PNG, WebP и GIF, отправленные как файл,
передаются модели вместе с подписью; фото без подписи тоже обрабатываются. При
выборе Gemini GIF автоматически преобразуется в MP4, поэтому модель получает всю
анимацию, а не неподдерживаемое статическое вложение. Telegram-«гифки», уже
сохранённые как MP4, передаются без преобразования. Статические WebP-стикеры
передаются целиком. При выборе Gemini короткие видео размером меньше 20 МБ
передаются исходным файлом
в форматах MP4, MPEG/MPG, MOV, AVI, FLV, WebM, WMV и 3GPP; подпись необязательна.
Голосовые сообщения до 20 МБ также передаются исходным аудиофайлом, поэтому
Gemini может прослушать речь и ответить на её содержание.
Исходный WebM-видеостикер также передаётся Gemini целиком, поэтому модель видит
движение, а не только один кадр. Для TGS-стикеров, других провайдеров или ошибки
загрузки бот использует растровое превью Telegram либо сам рендерит кадр. Вместе
со стикером передаются его тип и связанный emoji. Если медиа получить не удалось,
сообщение с подписью или описанием стикера всё равно доходит до модели как текст.
Остальные медиа без текста проходят ту же задержку чтения, но модели не передаются.

### Внутренний навык исходящих стикеров

Милана умеет сама отправлять стикеры из всех обычных наборов, установленных на
подключённом аккаунте. Каталог не добавляется к каждому запросу и не показывается
собеседнику. Если Милана хочет использовать стикер, она вызывает внутреннюю команду
`open_sticker_picker`: сначала получает контактные листы всех наборов, затем открывает
один из них и видит каждый его стикер со служебным ID и связанным emoji. Только после
этого можно вызвать `send_sticker` или `schedule_sticker`; придуманные и не показанные
в текущем выборе ID отклоняются.

Поддерживаются статические WebP, анимированные TGS и видео-WebM. Для контактных
листов сначала используется растровое превью Telegram, затем локальный рендерер, а
при повреждённом файле остаётся подписанная заглушка — стикер не исчезает из
каталога. Первое открытие может быть заметно медленнее из-за загрузки обложек;
дальше наборы и превью переиспользуются по Telegram hash-кэшу. Маски и наборы
кастомных emoji в этот каталог не входят.

Стикер может быть самостоятельным ответом, идти после текста или использоваться в
инициативном сообщении. Системная инструкция рекомендует один стикер, но технически
не запрещает осмысленную серию. Отложенная задача хранит постоянные ID набора и
документа, а перед отправкой заново получает свежий Telegram-документ, поэтому не
зависит от истёкшего `file_reference` и переживает перезапуск процесса.

### DEV-общение

Для отладки живого диалога запустите `ai-bot --dev-chat` или выберите отдельный
пункт DEV chat в `bot_control.bat`. В этом режиме входящее сообщение сразу
передаётся в обработку: отключены расписание и сон, симуляция статуса online,
ожидание тишины перед генерацией и паузы между частями ответа. Расписание также
не добавляется в инструкции модели и повторно не проверяется перед отправкой.

Память чата, дневник, поддержка фото, голоса, видео Gemini и стикеров, защита от
повторных событий и отмена устаревшего черновика при новом сообщении продолжают
работать. Режим выбирается при запуске процесса; для переключения работающего
бота сначала остановите его.

### Память и дневник

ИИ-ответчик хранит память локально в `data/milana_memory.sqlite3`. Для каждого
Telegram-чата сохраняются отдельные история и краткий обзор: сообщения и обзор
одного личного диалога не попадают в контекст другого. При первой синхронизации
импортируется вся доступная текстовая история чата в хронологическом порядке;
для длинного диалога это может занять заметное время только при первом запуске.

Несжатый контекст растёт от 300 до 500 сообщений собеседника. Вместе с ними
сохраняются связанные ответы Миланы, поэтому реплики не выпадают из середины
диалога. Когда в несжатой части накапливается 500 сообщений пользователя, модель
до формирования следующего ответа обновляет краткий обзор всего более старого
префикса и оставляет сырыми последние 300 сообщений пользователя. Таким образом,
вход основной модели всегда состоит из непрерывных частей: долговременного
обзора и полного свежего хвоста без пропусков и дубликатов. Активный входящий
пакет добавляется отдельно и ровно по одному разу.

Каждая реально отправленная часть ответа сохраняется отдельной строкой с
настоящим Telegram ID, поэтому прерванная серия и частичная ошибка отправки не
создают в памяти текст, которого собеседник не видел. В группах ключом памяти
остаётся весь `chat_id`, то есть участники одной группы используют общий контекст;
строгая изоляция по пользователю относится к личным диалогам.

Дневник Миланы общий для всех чатов. Модель сама вызывает функцию
`write_diary`, когда решает сохранить устойчивый факт, предпочтение, обещание
или важное событие. Записи дневника доступны ей в любом чате и сохраняются после
перезапуска программы. Служебный вызов функции пользователю не отправляется.
Промпт запрещает сохранять пароли, платёжные данные и одноразовые коды, однако
дневник всё равно является локальным чувствительным файлом — не публикуйте его и
ограничьте доступ к каталогу проекта. Каталог `data/` исключён из Git.

Активные очереди и ещё не отправленные черновики хранятся только в оперативной
памяти. После остановки процесса они не возобновляются как отдельные задания.
Существующий импорт Telegram-истории восстановит их как контекст при следующем
входящем сообщении, но не гарантирует самостоятельный ответ без нового события.

Во время сна Милана обычно не читает сообщения, не показывает «печатает…» и не
отвечает. Для каждого чата при первом ночном сообщении случайно выбирается порог
от 3 до 8 сообщений. Если собеседник достигает этого порога до утра, Милана
просыпается, читает весь накопившийся пакет и отвечает сонно и недовольно из-за
того, что её разбудили. Порог выбирается заново для следующего ночного ожидания.
Исключение — активная переписка, продолжившаяся после отбоя в пределах 30 минут
от последнего ответа Миланы: она идёт без механики пробуждения. Если сообщений
оказалось меньше порога, они не отбрасываются и при непрерывной работе бота будут
обработаны после обычного пробуждения и небольшой утренней задержки. Если за
время ожидания занятие сменилось, момент ответа пересчитывается уже по новому
занятию.

Во время обычного ожидания аккаунт остаётся «не в сети». Когда ответ готов к
отправке, Милана появляется онлайн. После последнего online или любого исходящего
сообщения диапазон задержки начинается с 1–10 секунд и за 15 минут плавно, по
нелинейной кривой, возвращается к диапазону текущего занятия. Новое появление online
перепланирует и уже ожидающие ответы. После каждого успешно отправленного ответа
статус гарантированно «в сети» ещё случайные 30–60 секунд. Время последней
внимательности хранится в SQLite и восстанавливается после перезапуска; при старте
оно также сверяется с последним исходящим сообщением аккаунта. Вне активной
переписки новый ответ не
начинается, если до отбоя осталось меньше минуты. При штатной остановке статус
переключается на «не в сети». Видимость статуса для конкретных людей также
зависит от настроек приватности «Последняя активность» в Telegram.

### Выбор LLM

Модель, которая пишет ответы, выбирается в `bot_control.bat`: откройте BAT-файл
и используйте пункт **3. Choose LLM model**. Доступны два варианта:

- OpenAI — модель из поля `model` в `ai_config.json`;
- Gemini 3.5 Flash — модель `gemini-3.5-flash` через Antigravity CLI `agy`;
  при исчерпании её лимита бот генерирует текущий ответ моделью OpenAI из
  `ai_config.json`, а следующий вызов снова сначала отправляет в Gemini.

Выбор сохраняется локально в `llm.choice` и применяется при следующем запуске
бота. Если бот уже работает, после смены модели выберите **Restart bot**.
То же самое можно сделать из терминала:

```powershell
.\bot_control.bat model openai
.\bot_control.bat model gemini
.\bot_control.bat restart
```

Для Gemini установите зависимости из `requirements.txt`, убедитесь, что команда
`agy` доступна в `PATH`, затем один раз запустите `agy` в обычном терминале и
войдите в Antigravity. Отдельный Gemini API key не требуется. Запросы запускаются
из одноразовой временной папки с `--sandbox`; история и медиа передаются
через временные файлы с абсолютными путями и удаляются после ответа. `agy` остаётся
внешним агентным CLI, поэтому этот режим не следует считать полной изоляцией ОС.

`agy` хранит постоянную OAuth-сессию в Windows Credential Manager. Код вида
`4/...`, который показывается после входа в браузере, одноразовый: не сохраняйте
его в `.env`. Для редких сетевых сбоев silent-auth используются безопасные
настройки повторов из `.env`:

```dotenv
AGY_AUTH_RETRIES=2
AGY_AUTH_RETRY_DELAY_SECONDS=1.0
```

Обычные текстовые запросы передаются сразу в `agy -p`, без дополнительного
чтения `request.json`; большой контекст, изображения и видео автоматически
используют файловый fallback. Текст быстрого запроса на время генерации виден в командной
строке процесса текущему пользователю и администраторам компьютера.

Добавьте в локальный `.env` API-ключ OpenAI (не публикуйте этот файл). Он нужен
как для основного режима OpenAI, так и для резервных ответов после исчерпания
лимита Gemini:

```dotenv
OPENAI_API_KEY=
```

Настройки модели находятся в `ai_config.json` рядом со скриптом:

```json
{
  "model": "gpt-5.6-terra",
  "system_prompt": "Ты — Милана. Отвечай дружелюбно и кратко.",
  "temperature": 0.7,
  "max_output_tokens": 1200,
  "message_flow": {
    "input_quiet_seconds": 2.0,
    "input_max_wait_seconds": 8.0,
    "max_reply_messages": 5,
    "inter_message_min_delay_seconds": 1.0,
    "inter_message_max_delay_seconds": 15.0
  }
}
```

- `model` — идентификатор основной модели OpenAI и резервной модели при
  исчерпании лимита Gemini; для обычных запросов Gemini автоматически
  используется `gemini-3.5-flash`;
- `system_prompt` — системный промпт; он может быть многострочным;
- `temperature` — вариативность ответов, от `0` до `2`; если выбранная модель
  не поддерживает параметр, клиент автоматически повторяет запрос без него;
- `max_output_tokens` — максимальная длина ответа, от `1` до `4000`.
- `message_flow.input_quiet_seconds` — сколько секунд тишины ждать после последней
  входящей реплики;
- `message_flow.input_max_wait_seconds` — максимальное дополнительное ожидание
  накопления пакета;
- `message_flow.max_reply_messages` — защитный предел смысловых сообщений в одном
  ответе (`1` отключает смысловое разбиение);
- `message_flow.inter_message_min_delay_seconds` и
  `inter_message_max_delay_seconds` — нижняя и верхняя границы паузы перед каждой
  следующей частью ответа. Внутри этих границ пауза считается по длине следующего
  сообщения: `0,8 секунды + количество символов / 11`. Всё это время собеседник
  видит индикатор «печатает…».

Каждую смысловую часть клиент дополнительно делит по техническому лимиту Telegram
в 4000 символов. Такое деление не влияет на решение Миланы о структуре ответа.

### Пульс и отложенные сообщения

Во время работы `ai-bot` Милана умеет ставить себе отложенные задачи на отправку
сообщения в текущий чат. Например: «напиши мне через 5 минут». Модель вызывает
`schedule_message`, сохраняет готовый будущий текст и задержку, а пользователю
сразу подтверждает постановку задачи.

Очередь пульса хранится в `data/milana_memory.sqlite3`, поэтому не теряется после
перезапуска. Фоновый пульс просыпается к ближайшему сроку, отправляет сообщение
даже без нового входящего события и сохраняет его в истории чата. При временной
ошибке Telegram используются до пяти попыток с увеличивающейся паузой. Пульс
работает и в обычном, и в `--dev-chat` режиме.

Измените файл и перезапустите `ai-bot`. Если `ai_config.json` отсутствует,
клиент использует прежние переменные `.env`: `OPENAI_MODEL` и
`AI_SYSTEM_PROMPT`, либо встроенные значения. Если при выбранной Gemini
`OPENAI_API_KEY` не задан, Gemini продолжит работать, но бот выведет предупреждение
о недоступности резервного ответа OpenAI.

## Расписание Миланы

Расписание хранится в `milana_schedule.json` и по умолчанию повторяет активный
студенческий пресет приложенного симулятора:

- будни: подъём в 07:30, отбой в 23:30, шесть часов учёбы, дорога, перерывы,
  прогулка, спорт и личное время;
- выходные: подъём в 09:30, отбой в 00:30, длинная прогулка, спорт и личное
  время;
- часовой пояс: `Asia/Yekaterinburg` (`UTC+05:00`).

Движок строит каждый день в той же последовательности, что HTML-шаблон, и по
тем же формулам считает энергию, стресс, продуктивность и баланс. Текущее
занятие, следующее занятие и показатели дня добавляются в инструкции модели
перед каждым ответом. Поэтому Милана отвечает на вопросы о том, что она сейчас
делает, с учётом реального времени.

Через случайный интервал от 30 до 90 минут обычный ИИ-бот запускает отдельное
инициативное событие. Модель смотрит текущее занятие, до 20 недавних личных
диалогов и их локальный контекст, а затем самостоятельно решает, хочется ли
Милане первой кому-то написать. Решение «никому не писать» является нормальным:
промпт специально запрещает механические и навязчивые сообщения. При
положительном решении модель выбирает только существующий личный диалог, создаёт
одно готовое сообщение, бот отправляет его и сохраняет в памяти чата. После
каждого события заново выбирается следующий интервал 30–90 минут. Группы,
каналы, боты, удалённые пользователи и сама Милана исключаются. В DEV-режиме
фоновое инициативное событие не запускается.

Задержки чтения и ответа тоже зависят от текущего занятия:

| Занятие | Когда Милана читает и отвечает |
|---|---|
| Сон | Не читает и не отвечает |
| Утренние сборы, завтрак | Через 15 секунд–2 минуты |
| Дорога | Через 15 секунд–3 минуты |
| Дорога домой | Через 30 секунд–5 минут |
| Учёба или работа | Через 4–24 минуты |
| Перерыв или приём пищи | Через 5 секунд–2 минуты |
| Прогулка | Через 45 секунд–6 минут |
| Спорт | Через 1–8 минут |
| Личные дела | Через 1–10 минут |
| Свободное время | Через 2 секунды–1 минуту |

Конкретный момент внутри диапазона выбирается случайно. Если раньше наступает
следующее занятие, старый таймер отменяется и диапазон выбирается заново для
нового состояния. Это не даёт длинной задержке с учёбы переноситься на уже
начавшийся перерыв и не позволяет ответу уйти во время сна.

Все параметры находятся в `milana_schedule.json`:

- `weekday` и `weekend` задают подъём, отбой, продолжительность учёбы/работы,
  прогулки, личных дел, дороги и перерывов;
- `custom_events` добавляет или перекрывает занятие в конкретный день и время;
- `response_behavior.default` задаёт общую задержку ответа;
- `response_behavior.by_kind` задаёт задержки по типу занятия;
- `response_behavior.by_title` задаёт более приоритетные правила по точному
  названию занятия;
- `online_behavior` управляет быстрым ответом и статусом «в сети».

Например, правила онлайна можно менять прямо в этом блоке:

```json
"online_behavior": {
  "online_response_min_seconds": 1,
  "online_response_max_seconds": 10,
  "attention_ramp_seconds": 900,
  "post_reply_online_min_seconds": 30,
  "post_reply_online_max_seconds": 60,
  "spontaneous_online_interval_min_seconds": 900,
  "spontaneous_online_interval_max_seconds": 2700,
  "spontaneous_online_duration_min_seconds": 120,
  "spontaneous_online_duration_max_seconds": 300,
  "sleep_buffer_seconds": 60,
  "conversation_sleep_delay_seconds": 1800
}
```

По умолчанию в доступное по расписанию время Милана заходит в сеть через
случайные 15–45 минут, остаётся там на 2–5 минут и снова выходит. Во время сна
такие фоновые появления не запускаются.

Минимальное значение не должно превышать максимальное, а
`sleep_buffer_seconds` должно быть не меньше
`post_reply_online_max_seconds`, иначе гарантированное online-окно может попасть
на сон.

Для статуса «в сети» быстрый диапазон намеренно ограничен 1–10 секундами.
`attention_ramp_seconds` задаёт, за какое время обе границы этого диапазона
плавно дорастут до границ текущего занятия; по умолчанию это 900 секунд.
После отправки ответа аккаунт остаётся online ещё от 30 до 60 секунд.

После каждого отправленного ответа Милана откладывает сон на
`conversation_sleep_delay_seconds` (по умолчанию 30 минут). Если плановый отбой
уже наступил, входящее сообщение, полученное до конца этого окна, продолжает
переписку; новый ответ заново отсчитывает полные 30 минут.

Параметры будней и выходных можно менять прямо в `milana_schedule.json`.
Дополнительные события добавляются в нужный день (`mon`–`sun`), например:

```json
"wed": [
  {
    "title": "Бассейн",
    "type": "sport",
    "start": "18:00",
    "end": "19:00"
  }
]
```

Пользовательское событие имеет приоритет при определении текущего занятия.
Секция `response_behavior` в том же JSON задаёт общий диапазон, диапазоны по
типу занятия (`by_kind`) и более точные исключения по названию (`by_title`).
Правило по названию имеет приоритет, поэтому, например, «Дорога домой» может
отличаться от обычной дороги. Пользовательские события автоматически получают
правило своего `type`. После изменения файла перезапустите ИИ-ответчик, чтобы он
загрузил новое расписание и правила ответа.

## Запуск через BAT-файл

Для обычного запуска используйте `bot_control.bat`: двойной клик открывает меню.
Из терминала доступны команды:

```powershell
.\bot_control.bat start
.\bot_control.bat dev
# Равнозначные варианты:
.\bot_control.bat start-dev
.\bot_control.bat start dev
.\bot_control.bat restart
.\bot_control.bat model
.\bot_control.bat model openai
.\bot_control.bat model gemini
.\bot_control.bat stop
.\bot_control.bat status
.\bot_control.bat logs
.\bot_control.bat web
```

Команда `.\bot_control.bat web` (или пункт **8** в меню) открывает локальную веб-панель управления Миланой в браузере. В панели доступны те же действия: запуск в обычном/DEV-режиме, перезапуск, остановка, переключение модели, просмотр текущего расписания и логов. Панель полностью повторяет функциональность `bot_control.bat`.

`start` запускает обычный режим с расписанием, а `dev`, `start-dev` и
`start dev` запускают DEV-общение с немедленными ответами. Эти два варианта
доступны отдельными пунктами и в интерактивном меню. Пункт выбора LLM и команда
`model` переключают OpenAI/Gemini; работающий процесс подхватит выбор после
`restart`.

Запуск выполняется скрыто. Идентификатор процесса сохраняется в `bot.pid`,
выбранный режим — в служебном `bot.mode`, выбранная LLM — в `llm.choice`, а последний вывод доступен в
`bot-output.log` и `bot-error.log`. Команда `stop` завершает процесс из
`bot.pid`; если сохранённого процесса уже нет, контроллер дополнительно ищет
локально запущенный `telegram_client.py ai-bot`.

Меню BAT-файла и команды `start`, `stop`, `status` показывают активный режим.
В обычном режиме ниже выводятся текущее занятие Миланы, его временной интервал,
оставшееся время и следующее занятие, а также энергия, стресс, продуктивность и
баланс текущего дня. При активном DEV-общении вместо расписания выводится явная
пометка о его обходе.

Для проверки отправки без беспокойства других людей используйте адресата `me`:

```powershell
python telegram_client.py send me Проверка подключения
python telegram_client.py read me --limit 5
```

Справка доступна через `python telegram_client.py --help` и через `--help` после
любой команды.
